<?xml version="1.0" encoding="utf-8"?>
<documentation>
  <language culture="en" />
  <language culture="de-DE" />
  <introduction lang="en">The Gate controls the execution flow in complex workflows. It passes a data object unchanged from the input to the output, if it receives the correct trigger on the control input. After the object has been forwarded, the Gate is closed again and waits for the next input object and trigger.
</introduction>
  <introduction lang="de-DE">Das Gate kontrolliert den Ausführungsfluss in komplexen Szenarien. Es reicht ein Eingabeobjekt unverändert weiter, sobald es einen Trigger auf dem Kontrolleingang erhält. Nachdem das Eingabeobjekt weitergeleitet wurde, ist das Gate wieder geschlossen und wartet auf das nächste Eingabeobjekt und den nächsten Trigger.
</introduction>
  <usage lang="en">Set up the trigger type in the Gate parameters. Usually you will want to wait for a boolean "true" value. If you set it to "no trigger", the control input will be ignored and any input object will be either forwarded instantly (always open) or not at all (always closed).
</usage>
  <usage lang="de-DE">Die Art des Triggers kann in den Gate-Parametern eingestellt werden. Üblicherweise wird ein boolscher "true"-Wert verwendet. Wenn "kein Trigger" ausgewählt ist, wird der Kontrolleingang ignoriert und alle Eingabedaten werden entweder sofort weitergeleitet (immer geöffnet) oder gar nicht (immer geschlossen).
</usage>
  <presentation lang="en"></presentation>
  <presentation lang="de-DE"></presentation>
  <references></references>
</documentation>